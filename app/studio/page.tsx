/**
 * app/studio/page — Studio tabbed interface entry point.
 */

'use client';

import { StudioShell } from '@/components/studio/StudioShell';

export default function StudioPage(): React.ReactElement {
  return <StudioShell />;
}
